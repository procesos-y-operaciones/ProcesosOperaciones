<%= form_with(model: goals_user, local: true) do |form| %>
  <% if goals_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goals_user.errors.count, "error") %> prohibited this goals_user from being saved:</h2>

      <ul>
      <% goals_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
