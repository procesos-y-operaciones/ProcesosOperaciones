<%= form_with(model: charges_goal, local: true) do |form| %>
  <% if charges_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(charges_goal.errors.count, "error") %> prohibited this charges_goal from being saved:</h2>

      <ul>
      <% charges_goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
