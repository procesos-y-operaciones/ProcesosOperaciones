<%= form_with(model: identification_type, local: true) do |form| %>
  <% if identification_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(identification_type.errors.count, "error") %> prohibited this identification_type from being saved:</h2>

      <ul>
      <% identification_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
