<%= form_with(model: competencies_evaluation, local: true) do |form| %>
  <% if competencies_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(competencies_evaluation.errors.count, "error") %> prohibited this competencies_evaluation from being saved:</h2>

      <ul>
      <% competencies_evaluation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
