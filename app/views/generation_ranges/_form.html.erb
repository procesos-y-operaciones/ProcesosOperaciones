<%= form_with(model: generation_range, local: true) do |form| %>
  <% if generation_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(generation_range.errors.count, "error") %> prohibited this generation_range from being saved:</h2>

      <ul>
      <% generation_range.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
