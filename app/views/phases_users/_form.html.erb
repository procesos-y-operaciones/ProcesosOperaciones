<%= form_with(model: phases_user, local: true) do |form| %>
  <% if phases_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phases_user.errors.count, "error") %> prohibited this phases_user from being saved:</h2>

      <ul>
      <% phases_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
