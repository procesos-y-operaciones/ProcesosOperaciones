<h2 class="center second"><%= t('general.messages.create') %> <%= t(@period.class.model_name.human) %></h2>

<%= render 'form', period: @period %>

<%= link_to t('general.buttons.back'), periods_path, class:"btn btn-default" %>
