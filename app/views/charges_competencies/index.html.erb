<p id="notice"><%= notice %></p>

<h1>Charges Competencies</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @charges_competencies.each do |charges_competency| %>
      <tr>
        <td><%= link_to 'Show', charges_competency %></td>
        <td><%= link_to 'Edit', edit_charges_competency_path(charges_competency) %></td>
        <td><%= link_to 'Destroy', charges_competency, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Charges Competency', new_charges_competency_path %>
