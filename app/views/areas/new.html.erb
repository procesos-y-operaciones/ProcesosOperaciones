<h2 class="center second"><%= t('general.messages.create') %> <%= t(@area.class.model_name.human) %></h2>

<%= render 'form', area: @area %>

<%= link_to t('general.buttons.back'), areas_path, class:"btn btn-default" %>
