<h2 class="center second"><%= t('general.messages.edit') %> <%= t(@goal.class.model_name.human) %></h2>

<%= render 'form', goal: @goal %>

<%= link_to t('general.buttons.back'), goals_path, class:"btn btn-default" %>
